<div class="flex flex-col sm:flex-row justify-between items-center pb-5 sm:pb-0">
  <div id="<%= dom_id message %>" class="w-full bg-blue-200 px-4 rounded-md sm:w-auto my-5 space-y-5">
    <div class="flex gap-5 items-center">
      <div>
        <strong class="block font-medium mb-1"><%= message.user.name %>:</strong>
      </div>
      <div>
        <%= message.content %>
      </div>
    </div>
  </div>
</div>
