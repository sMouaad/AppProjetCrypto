<h1>Your Conversations</h1>
<ul>
  <% @conversations.each do |conversation| %>
    <li>
      <%= link_to "Chat with #{conversation.sender == current_user ? conversation.receiver.email : conversation.sender.email}", conversation_path(conversation) %>
    </li>
  <% end %>
</ul>
